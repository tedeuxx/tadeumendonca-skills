# Methodology ADRs

Architecture Decision Records for the **dev-loop machine** — the reusable engineering methodology this
plugin exports. Product decisions live in each consuming repo's own `docs/adr/` (e.g.
`tadeumendonca-io/docs/adr/`), not here.

Practice and template: [`/documentation-standard`](../../skills/workflow/documentation-standard/SKILL.md)
(Part II — merged from the former standalone `adr` skill at #260) · [`template.md`](./template.md).

| ADR | Title | Status |
|---|---|---|
| [0001](./0001-adopt-madr-adrs.md) | Adopt MADR Architecture Decision Records | accepted |
| [0002](./0002-agentic-dev-loop-architecture.md) | Agentic dev-loop architecture (per-task subagents, ADRs-as-brain) | accepted · amended 2026-07-23 (`product-owner`; then `product-manager` · `analytics` · `debugger`) · **amended 2026-07-24** (amendment #3 — roster reshape: `product-owner` re-scoped, `brand-guardian`/`editor`/`recruiter`/`scrum-master`; owner-ratified, implementation sequenced per #69) · **amended 2026-07-29** (amendment #4 — the `brand-guardian` trigger is a fail-closed rule, not a path list) · **amended 2026-07-30** (amendment #5 — `product-manager` gets a trigger; the reviewer's output gets a round budget) · **amended 2026-08-01** (amendment #6 — a finding blocks only by naming a criterion and a falsifier; DoD criterion 10; the lenses self-classify severity) · **amended 2026-08-02** (amendment #7 — roster 19 → 6 on a new criterion: a persona exists only where conflict is wanted) · **amended 2026-08-02** (amendment #8 — the intake chain; both gatekeepers approve every MR in parallel; the builder delivers the E2E suite) · **amended 2026-08-04** (amendment #9 — `marketing-lead` merges into `product-lead`, roster 6 → 5; the blocking-truth clause carries across explicitly, the capability floor behind it does not) · **appended 2026-08-04** (amendment #9's accepted cost is **closed** — the remedy it pre-committed to, a tool grant, is struck in favour of an `agent_type`-keyed deny that keeps `Bash` and removes publishing; the `/architecture` obligation is booked in ADR-0004) · **appended 2026-08-04** (amendment #6 item 2's summary of criterion 10 is **superseded** — the criterion no longer passes on *"the lens returned a verdict"*; see ADR-0006's third 2026-08-04 amendment. Its second half, the reviewer's own falsifiability clause, stands) · **amended 2026-08-04** (amendment #10 — **`harness-lead` joins tier 1**, the owner's pair on the machinery: advisory, pre-implementation, gates nothing, standing rule *every scenario ships with how to verify it or is labelled a hypothesis*. **`security` is absorbed into `quality-assurance`**, which holds two lenses in one pass and labels every finding with its lens — owner's decision, reaffirmed after objection, for fewer profiles reconciling one result on the same MR. **The roster is still five and two members changed.** Four costs booked, the structural one being that **nobody now observes the gate that signs the merge**, which is why the merged persona did not inherit `Edit`. The persona criterion widens from *conflict wanted* to **four reasons**, with **reconciliation cost paid within a tier**. Amendment #9's *"both approvals are still required"* is **struck** — a record describing a control as stronger than it is. And the rule the gap leaves behind: **a count is not an identity**, since swapping one persona for another held `inventory-counts` at five and every gate stayed green. **An omission, not a policy** — the previous roster change amended four records in the same commit) · **amended 2026-08-12** (amendment #11 — Decision 1's *"advisory, pre-implementation"* clause is **struck** rather than rewritten, on the owner's reversal: `harness-lead` gains an implementer role over [ADR-0015](./0015-harness-lead-implements-the-harness-it-reviews.md). The **merge** and **MR-review** clauses of Decision 1 stand verbatim and unchanged — `harness-lead` still never merges and never gates an MR; only its pre-build-only limit is reversed) |
| [0003](./0003-mr-definition-of-done.md) | Merge Request Definition of Done | accepted |
| [0004](./0004-autonomy-and-permission-model.md) | Autonomy & permission model (classes, tool-scoping) | accepted · **amended 2026-07-25** (the agent-scoped merge gate — rule 7b — makes "only the reviewer merges" mechanically true, #77) · **amended 2026-08-02** (where mechanism belongs and where a skill carries the rule instead; the accurate `agent_type` property is *cannot claim*, not *cannot obtain* — these rules enforce **routing**, #125) · **amended 2026-08-03** (the main agent's ASK on `gh issue create` is removed — visible-by-construction versus invisible; the subagent deny is untouched) · **amended 2026-08-04** (per-persona scoping now has a **second** surface — an `agent_type`-keyed deny in the floor alongside the `tools:` frontmatter; effective capability is the grant *minus* the denials; books the obligation on `tadeumendonca-io`'s `/architecture`) · **appended 2026-08-04** (5e's orphaned consequence is closed by ADR-0006's decided relay — the *act* still has no destination, the *content* now does, and the separation the rule buys is exactly as wide as before; a persona-keyed publication deny must name the receiver of that persona's output in the same MR) · **amended 2026-08-04, second** (what each layer of the floor actually stops — the deny list holds the direct form, the hook holds the wrapped form, **neither is a sandbox**; its layering half is superseded by [ADR-0008](./0008-which-layer-carries-a-control.md), and **its opening decision — *"`Bash(bash:*)` and `Bash(sh:*)` stay in the committed floor"* — is superseded in place later the same day**: the owner took the interpreter class out of `allow` (`14d7b43`, `786437c`) once plain string concatenation showed a fourth patch to the unwrap regex buys a spelling and not the class. Non-containment stays accepted; `node`/`python3` stay granted, which is why removing the rest is a change to what is *free*, not to what is *contained*) |
| [0005](./0005-plugin-auto-versions-on-merge.md) | The plugin auto-versions on every merge; adoption is the consumer's opt-in | accepted · **amended 2026-08-10** (adds the *which part* axis for one case the record left to `CLAUDE.md`: **a follow-on PATCH may carry the remainder of a break whose first half already shipped under a MAJOR** — [#164](https://github.com/tedeuxx/tadeumendonca-skills/issues/164)'s split travelling on top of `1.0.0`, decided by the owner and **recorded as an interpretation**, with both readings kept and neither provable from the tree: *one contract change cut halfway* (chosen — the rename rule was **satisfied at `1.0.0`, not waived**; `7590a14` carried no content, it was an announcement) against *two separate breaks* (`#174` removed four commands, the split renames sixty-nine). **The additive escape is explicitly unavailable** — `commands/` holds 2 files, `skills/` 69, and the names change — so on the second reading this is a knowing deviation. Affordability is a **circumstance, not a rule**: the one consumer is pinned per-version in `installed_plugins.json` and a pin is a lockfile; any future appeal must re-derive the consumer set. Books the capability change the number does not carry (**`Skills (2)` → `Skills (71)`, +9,919 always-on tok/session**, relayed from #182) and weighs the **second renamed surface** — `skills:` preload identifiers go family-qualified → bare — as **fact-strengthening but part-neutral**, since its only author today is `agents/**` here and `skills-resolve.test.sh` assertion 5 reddens on the old form. **Rules on the release-note obligation:** `quality-assurance`'s proposed assertion belongs **nowhere in its proposed form** — `version-main.yml:57-91` **generates** the whole body from commit subjects with `--no-merges`, so no one authors notes, the PR title never appears, the first line is a section heading (`### 🐛 Fixes` for this branch) and the check would fire after the tag is pushed, which ADR-0008 routes to the wrong layer; a follow-up may re-specify it at **PR time on commit subjects**, not pre-approved. What keeps the obligation alive is named as weak on purpose: a post-publish `gh release edit --notes-file`, with the pull-in-the-interval cost booked. Rejected: `2.0.0` (with the cost of being wrong stated) · a MINOR (asserts a compatibility that is false — it buys signal by making the number lie) · hand-authored notes (the generator overwrites unconditionally). **The trigger decision, the auto-patch model and every original consequence stand unchanged**) |
| [0006](./0006-a-verdict-owed-to-another-persona-is-an-artifact.md) | A verdict one persona owes another is an artifact on the PR, not a relayed claim | accepted · **amended 2026-08-03** (both gatekeepers granted a scratchpad-scoped `Write`; the load-bearing `--body-file` question inside *Consequences* is closed — measured: a ~60-line verdict posted with every backtick hand-stripped) · **amended 2026-08-04** (the *closing* open question's premise is falsified by ADR-0002 amendment #9 — `marketing-lead` no longer exists and the copy lens now holds `Bash`; the hole it named survives) · **amended 2026-08-04, second** (rule 5e now stands behind the copy lens's identifier-only rule, which **prices** the third-marker question rather than settling it; flags that 5e's deny message cites this record for a relay mechanism it does not name) · **amended 2026-08-04, third** (**the closing open question is CLOSED** — a gate **may** relay another persona's verdict and, where a criterion waits on it and the floor denies that persona every route to publish, **must**; `quality-assurance` quotes the copy verdict **verbatim under its own marker** and criterion 10 upgrades from *returned* to *returned and quoted*. Accepted cost: the carrier merges, so attribution is weaker than a first-party artifact and selective quoting has no detector. The **multiplier objection survives** and is now the standing reason not to revive the third marker. Evidence: the alternative was measured at **five omissions in one session**. Relaying `security`'s approval or the owner's ratification remains forbidden) · **amended 2026-08-04, fourth** (**the gate-reads-gate verification has lost its subject** — `security` is absorbed per ADR-0002 amendment #10, so the remaining verdict is posted by the party that merges and **read by nobody**. The artifact survives and still closes *omission*; **confirmation** is gone, and *"verified"* should not be written of the remaining gate. The exclusion list loses `security`'s approval as an entry and keeps its principle — **authority versus record** — with the owner's ratification now its only member. The relay decision and the multiplier objection are untouched). **The decision itself is unchanged by all five.** |
| [0007](./0007-the-merge-precondition-is-a-floor-not-an-instruction.md) | The merge precondition is a floor, not an instruction — a hook on the decidable half (the markers): a missing tool **denies** before either question is asked; otherwise it **allows** only on an affirmative answer, **asks** only where the check could not run and no outsider could have caused that, and **denies** in every remaining case — the read is bounded, and the ratification half stays prose | proposed · qualifies under ADR-0004's **decided** rule (a merge is a push to the trunk) · reverses ADR-0006's rejected option 2 · **amended 2026-08-04** (the precondition is **ONE** marker, not two — `security` is absorbed per ADR-0002 amendment #10. **Status unchanged**: the hook is unimplemented, so nothing running is affected and the amendment protects the slice that would have implemented it. The decision, the author filter and all four outcome rows stand; the **argument gets stronger**, because the surviving marker belongs to the merging gate itself and ADR-0006 has just recorded that verdict as self-enforced. The impersonation residue is unchanged and now covers the whole precondition) |
| [0008](./0008-which-layer-carries-a-control.md) | Which layer carries a control — the hook is **authoritative**, the settings `deny` list is the floor for the **direct form**, and the authoritative layer **fails open** | accepted · **supersedes** the layering claim in [ADR-0004](./0004-autonomy-and-permission-model.md)'s second 2026-08-04 amendment (inverting the *"settings.json is the hard backstop"* claim the hook's own header carried since it was written). Routing test: **wrapped · composed · semantic · shadowed by an `allow`** → the hook; everything else → the floor. Rejected: **fail-closed** (a missing `jq` wedges the loop with no repair route that avoids the owner — measured on `wip-guard.sh`) and **pattern-listing every spelling** (~150 probes found **nine** unlisted spellings across rules already swept). Accepted costs: the authoritative layer fails open and the set it alone carries only grows; and **the retained floor entry is the fail-closed half of a two-layer control**, held by convention with **no mechanism that could check it without being wrong more often than right** · **amended 2026-08-04** (two corrections that leave the routing decision, the fail-open acceptance and the retained-floor-entry cost standing. **(1) The tense:** the record priced the interpreter perimeter as inherited, and `bash`, `sh`, `xargs` — plus `gh -R`/`gh --repo` — enter `allow` in the *same diff*; measured against the settings diff and the pre-truncation overlay, `perl`/`ruby` were already effectively granted but `bash`'s `-c` class was not, so *Context* item 1's hole is one this batch **opened** in the committed floor. **~~Outward~~ — corrected by the second amendment below: all seven entries came back out before the batch ended.** **(2) The word:** *closed* is not available to a control that matches a **pattern against a caller-controlled string** — only *"these spellings, measured this way, on this date"*, because the class is a grammar and the evidence is always a sample. Third occurrence in one batch; ten spellings still reached ALLOW at `ce2deea`, including the merge gate under `$'…'` and every `bash <option> -c` form. Costs booked: longer and unquotable records, a measurement to check instead of a verdict to read, and **no stopping rule** — that last one unremedied and stated as the strongest argument against. Scope is narrow: a **closed, enumerable** domain such as `agent_type` may still be recorded as closed) · **amended again 2026-08-04, second** — the record asserted a floor three later commits in the same batch had dismantled: at `cb9a2f3` **none** of `Bash(bash:*)`, `Bash(sh:*)`, `Bash(xargs:*)`, `Bash(perl:*)`, `Bash(ruby:*)`, `Bash(gh -R:*)`, `Bash(gh --repo:*)` is in `allow` and the committed floor is **identical to `main` on all seven**. The rule it earns: **removing a floor entry is a change to at least five artifacts, and the entry's literal name is the only thing that finds them all** — settings, this record, ADR-0004, the hook header, the index, the two suites; every removal commit was scoped to the file it edited plus the adjacent narrative, and the drift sits exactly at the sites they did not open. Also books two measured residuals: the `Edit(.claude/**)` control is void **by scope** (a repo's `settings.json` is not loaded at all in a session rooted elsewhere) on a tool **no hook can see** (`PreToolUse` is registered on `Bash`), so no cheap fix is being deferred; and `gh <sub> --repo <o/r>` **matches** the per-subcommand entries, which is why withdrawing the two flag prefixes cost nothing) · **amended 2026-08-07** (records the floor grant `Bash(bash .scratch/*)` as what it is — `bash <any path on disk>` with a required prefix token, **nothing bounding it**: not the matcher, which cannot express a directory, and not `permission-guard.sh`, which does not resolve paths. Measured against the floor and the hook **as this MR leaves them**, `bash .scratch/.""./.""./<other-repo>/VERSION` draws **no decision from any layer** — there is no `..` adjacency anywhere in that string, so no pattern reaches it. The naive `bash .scratch/../../<other-repo>/VERSION` is **denied** at this head by rule 9, which this same MR adds, and is deliberately **not** the evidence offered: quoting it would hand the reader, as proof, the one string this MR closes. It is a **project**-floor entry — absent from `~/.claude/settings.json`, mirrored into the consumer at `-io`#371 — and the record gives the **grep** rather than a count of which repos carry it. The reach it costs is **nothing already ungranted**, and deliberately **not** stated as a list of interpreters: `Bash(command:*)`, `Bash(awk:*)`, `Bash(find:*)` and `Bash(sed:*)` each reach an arbitrary interpreter one wrapper over — `command perl -e` runs undecided though neither `perl` nor `ruby` is allow-listed — so *which interpreters are allow-listed* is the wrong question, confidently answered. The claim is stated in its **falsifiable** form — **at least one `allow` entry reaches an arbitrary interpreter, so reach is already unbounded**, settled by one probe — rather than as a claim about enumeration effort, which has no falsifier; the four entries are a **sample, not the set**, and closing any of them restores no bound. Also recorded, because both are true of what this MR does: the spelling is **relative**, a deliberate deviation from #155's absolute per-repo DoD, on the owner's ruling (*relative works, so he prefers it*) — **preference, recorded as preference**, after an author's cost case against the absolute form collapsed on measurement; and the **net floor is BROADER** after this merges, because `Bash(bash /private/tmp/*)` stays live in the **unversioned user-scope** settings over world-writable `/private/tmp` — **noted without being owned**, since no MR can reach that file)
| [0009](./0009-a-skill-description-is-a-trigger-not-a-title.md) | A skill's `description` is a **trigger**, not a title — the canonical form every skill is written to, and an enforcement boundary that refuses a quality score | accepted · the standard for all 75 skill descriptions and every one added from now on, moved out of a comment on a closed Issue (#166) into the library, because **a comment cannot hold a control**. Form: act + object + **where in nouns, never the folder** · a mandatory **`Use when`** · a cluster-scoped **`Not for … (see X)`**, mutual, across six clusters on three axes (**use vs provision · which surface · decide vs implement vs verify**). Measured at `593886b~1`: **2 of 75** files had a `description`, **73 had no frontmatter at all**. Rejected: **reusing the existing first line** (those lines are the defect — titles, and 305–485 chars against a 150-char table cap) and **any quality score** — keyword count, noun density, embedding similarity — refused **by name**, because all three pass on keyword salad, which is the failure they would be bought to catch. Two things the Issue comment does not carry. **(1) The plain-path deviation is upheld and its tension is left OPEN**: `see backend/metrics` rather than a backticked bare stem, because it is **machine-resolvable** — re-derived at `596481e`, **112** pointers resolve with **zero** dangling, and under bare stems that check could not exist, since **four stems occur in two families each** (`cloudwatch-rum`, `coverage`, `dynamodb`, `environment-config`) and one such pair straddles its own cluster's separating axis. The cost: it **embeds a folder path**, exactly what the layer-in-nouns rule warns against, so **#164 can rot 112 pointers in one merge**. Recorded as a dated tension and deliberately **not** resolved — #164 is undecided and a sentence's punctuation must not decide the tree's shape. **(2) The premise is UNVERIFIED**: the authorial half — is the situation the right one, is `not for` the nearest rival, is the description **true about the body** — has no gate and can have none, and the standard's own instrument for it is **a dispatch, not an assertion** (~10 real task sentences, which skill each matches). **It has never been run**, and it is also the only thing that would confirm that model-invoked loading matches on `description` at all · **amended 2026-08-09** (the *frontmatter-scoped* consumer-path ban is now **whole-file**, widened by [PR #169](https://github.com/tedeuxx/tadeumendonca-skills/pull/169) under [#167](https://github.com/tedeuxx/tadeumendonca-skills/issues/167). The original scope was a **sequencing decision, not an oversight** — re-derived at `485b97e`, **80 occurrences on 73 lines across 45 files**, so a file-wide assertion would have arrived already red, which ADR-0008 prices as a check that gets silenced. Discharged: bodies to **zero**, mutation-proven at `commands/frontend/state.md:28`, a line the old scope structurally could not see. The distinction that decided seven judgement calls: **`tadeumendonca-skills` is the plugin naming its own invocation surface and stays; `tadeumendonca-io` and `tadeumendonca.io` are one consumer's repo and domain and go** — hence an exact-token allowance, not a prefix. Two things ruled OUT of this record. **(1)** `<project>-pwa` / `<project>-iac` survive in **11** skills: placeholdered, so the check passes correctly, yet asserting one two-repo topology as the pattern — **zero of them are in a description**, so it is neither this record's nor #167's, but a content question beside #164, and the owner's to open. **(2)** Option 2's rejection left **two independently authored descriptions per skill** — the field, and the first body line that `skills-table.py` publishes as README column 2 — and `inventory-counts.test.sh` keys that table on cells **1 and 3 only**, so a fabricated description cell passes: mutation-proven, **62 passed, 0 failed**. Booked as a **cost**, not a decision; the fix is a routine regenerate-and-diff assertion, below the significance bar, and folding it in would put a second decision in a one-field record) · **amended 2026-08-09, second** ([PR #174](https://github.com/tedeuxx/tadeumendonca-skills/pull/174), step 1 of [#164](https://github.com/tedeuxx/tadeumendonca-skills/issues/164) — the four cross-family pairs merged). **The decision, the seven constraints, the cluster rule and the refused quality score all stand; two of the record's load-bearing measurements do not.** Measured at `78f4e5b`: **zero** stems now occur in two families (was four), so **the argument this record called *decisive* for the plain-path `see backend/metrics` form has evaporated** — all 71 stems are unique and a bare stem would resolve just as deterministically. **The form is upheld anyway**, on the two reasons the record ranked weaker: constraint 7 forbids markdown, and the 98 live pointers are already written this way. **The tension has INVERTED and that is the part a flattening MR needs** — the bare stem was unavailable before and is now the spelling that survives a flat tree, so step 3 faces a mechanical `family/stem` → `stem` rewrite rather than a choice between rot and ambiguity; still **not** resolved here, deliberately, because #164 step 3 is undecided. **Pointer count: 110 → 98** by the one instrument that still exists, and the record's **112** is **not** corrected to 110 — the original `.scratch/verify-pointers.py` was discarded, the two-pointer gap is unrecoverable, and both figures are recorded with the surviving command so the next reader re-derives. Cluster table **31 → 27** members. **Discharged:** the booked cost of four superseded descriptions — the four third descriptions exist and pass the gate. **Newly booked, and unforeseen:** all four merged skills now **span the very axis their cluster separates on** (`use vs provision` for `dynamodb` and `cloudwatch-rum`, `which surface` for `coverage` and `environment-config`) — **the families WERE the axis**, so the owner's merge ruling necessarily produces straddlers; the rule survives because reciprocity still holds, but those four lose one axis of separation. Accepted as a cost, since inventing an axis to fit a check is the shape option 3 was refused for. **Ruled *no ADR*, explicitly:** PR #174's family-choice rule (*charter, then inbound references*) killed four published invocation names, and the deaths are **entailed** by the owner's already-recorded *merge rather than differentiate* ruling — the rule chose **which** name, not **whether**, and it governs a taxonomy #164 step 3 abolishes, so no cross-cutting pattern arm fires |
| [0010](./0010-a-personas-startup-context-is-a-curated-preload.md) | A persona's startup context is a **curated preload** — each brief declares the skills it loads in full, nothing else in the library reaches it, and the identifiers are asserted in CI because a wrong one is silent | accepted · `skills:` **injects each named file's body** before the persona's first turn, and it is the **only** channel: `Skill` is not grantable through `tools:` ([#177](https://github.com/tedeuxx/tadeumendonca-skills/pull/177), which removed [#171](https://github.com/tedeuxx/tadeumendonca-skills/issues/171)'s inert grant) and `printenv CLAUDE_PLUGIN_ROOT` **exits 1 inside a subagent shell**, re-derived in the dispatch that wrote the record. So **every exclusion is a real deprivation, not a deferral**, and each brief argues its own omissions. Five lists, **79,261 B as billed, 63,647 B distinct — 14.2% of 71 files / 449,205 B**, none over 35 KB: `developer` 35,294 · `quality-assurance` 18,215 · `tech-lead` 16,857 · `product-lead` 8,895 · `harness-lead` **0, `skills: []` written out** because an absent key is the same glyph as a dropped one. Two entries are decisions rather than gaps: the gate does **not** get `principles:engineering-philosophy`, since its ruler is external and a principles document is a ruler with no falsifier; and the empty list is deliberate, because a preload is a frozen snapshot handed to the persona whose rule is *read the files*. Rejected: **preload nothing** — the incumbent, which looked like a deferral only while #171's grant was believed to work, and actually leaves four of five personas unable to reach any skill; and **the universal floor** (*"every persona loads `/principles/*`"*, [`docs/proposals/agentic-dev-loop.md:253`](../proposals/agentic-dev-loop.md) — header at `:251`, follow-up at `:260`), which allocates by family where the cost is bytes: **76,490 B per persona, 382,450 B across five**, one member (`dev-loop`, 38,702 B) larger than the whole list of the persona that would want it, and it would hand the gate the second ruler above. **The proposal is not rewritten — this record is the pointer that stops a superseded proposal reading as instruction.** Identifiers are colon-separated, no slash forms, no globs, **no dedupe**, and a wrong one fails at **0 bytes of stderr** — so per [ADR-0008](./0008-which-layer-carries-a-control.md)'s routing test the control cannot sit in the runtime and lives in `hooks/scripts/skills-resolve.test.sh`, with `agents/**` added to the CI path filter — **and that
was one of the three entries it needs**, corrected on #164: the suite resolves identifiers against the
library too, so `commands/**` (#180) and `skills/**` (#164, when the library moved there) are equally
load-bearing and a rename on the target side is what the single entry could not start the gate on. Costs: **the curation has no falsifier**, the list is **static** (no per-dispatch top-up — `developer` works a CI slice without `workflow:github-actions`), nine of the ten entries carry a family segment and get rewritten if [#164](https://github.com/tedeuxx/tadeumendonca-skills/issues/164) flattens the tree, `backend:coverage` gains a third publication under a stem that misdescribes it, and the repo now publishes **two** allocations — *whose domain* (unchecked) and *what is preloaded* (checked) — which visibly disagree, deliberately. [ADR-0002](./0002-agentic-dev-loop-architecture.md) and [ADR-0009](./0009-a-skill-description-is-a-trigger-not-a-title.md) **cited, not amended** — a preload **bypasses** description-based discovery rather than correcting it |

| [0011](./0011-a-skill-exists-to-be-assigned-to-a-profile.md) | A skill exists to be **assigned to a profile in the loop's roster** — *to which profile is this assigned, and why?* is the operative test, and a skill assigned to nobody has no reason to exist whatever its quality; what it standardises is a **behaviour**, transversal, persisting across sessions | accepted · **THE OPERATIVE TEST is assignment**, and it is deliberately not *is this good* / *is this generic* / *is this a standard* — **a file that cannot name a profile does not belong regardless of quality**, because *a library grew for two years against no assignment criterion and the defect stayed invisible since every individual file was defensible*. **Measured: 5 profiles, 69 skills, 7 assigned, 62 unassigned — 90%.** `developer` 3 · `quality-assurance` 3 · `tech-lead` 3 · `product-lead` 1 (`new-issue`, **a command, which must go**, leaving it empty) · `harness-lead` 0; **8 of 14 process skills assigned to nobody**, `dev-loop` among them. **One figure corrected on measurement rather than published round:** *"none of the ~55 technical files is assigned"* is **false by one** — `backend/coverage` is assigned, **to `quality-assurance`, the profile that CHECKS, not to `developer`, which builds** and carries three process skills and **zero** delivery standards; the exception is the finding, and the rounder claim would have hidden it. **Consequence for what the plugin IS: the library is the roster's equipment**, sized by what five profiles need, not by accumulated knowledge — and what cannot answer the assignment question is an **archive** (publishable, referenceable, not a skill), a named destination so the ordered review is not forced into *keep or cut*. **Prevention is stated as ABSENT:** `skills-resolve.test.sh` asserts identifiers resolve to files and **nothing asserts the reverse** — that a file is consumed by some profile — which is exactly how 62 files arrived; the closing assertion is a few lines, **would fail on 62 of 69 today**, and is deliberately **NOT decided here**, since a check arriving red on 90% of its subject is one this repo has already paid to learn gets silenced (ADR-0009's first amendment). *Review, then assert.* · the owner's definition quoted **unparaphrased** because each of his sentences corrected a framing that had been offered and was wrong (*norm versus knowledge* · *stack-free versus generic* · *repeated behaviour versus transversal* · *within that stack* · *centralisation is one definition, not also delivery*). **Two SUBORDINATE tests decide whether a file's CONTENT is a skill's content once a profile exists — not whether the file should exist, which only assignment decides — and each is recorded through a candidate it REJECTS**, since a criterion that only admits settles nothing: *does this change what an agent does?* rejects the accurate, generic, well-written passage that changes no behaviour — a class no measurement on #183 could see; *is it transversal?* rejects `--squash` (`grep -c squash agents/*.md` → **1 file, `quality-assurance`, the only persona that merges**), which a *repeated-behaviour* framing wrongly flagged as under-covered. **Scope is free and is NOT a quality signal** — a technical skill may anchor one stack (`vpc`), several, or none (`coverage`, `iam`); the technology in the name is the **scope of the behaviour, not the subject of the file**, so *the more a technical skill reads like documentation about the technology, the less of a skill it is*. **Generic means workload-free, not stack-free:** the project-agnostic lint is green on all 69 (re-derived → **0**) and cannot see shape — `skills/backend/lambda-handler/SKILL.md:7` reads `Module: $ARGUMENTS (e.g., "posts", "articles", "notifications")` and has **no project name to substitute**; a vocabulary grep is not the replacement (44 files hit, including `principles/dev-loop` for the verb *posts*), so **the class is a review judgement with no mechanical detector, said plainly rather than papered over with a regex**. Three surfaces separated: **command** = the owner acting, now · **brief** = the orchestrator instructing, this session · **skill** = the owner defining, every agent every session — so **a persona must never preload a command**, which `agents/product-lead.md` does today (`new-issue`), and removing it leaves that list **empty**. Association is **transversal** — no persona owns a skill — and `developer` today preloads three methodology files and **zero** delivery standards. Rejected: **duplication across the five briefs** — the incumbent, and it **was the control**, correctly, while the loader reported `Skills (2)` before [#182](https://github.com/tedeuxx/tadeumendonca-skills/pull/182) ([ADR-0005](./0005-plugin-auto-versions-on-merge.md)`:146-149`); it expired 2026-08-10 and its cost is now measured drift (**2 clauses live only in `dev-loop:475,477` and 0 in any brief — including *a change to the loop's own rules is boundary class*, absent from the one persona that classes**) at **20,777 B** re-derived · **a universal floor of all `principles/*`** (**76,059 B ×5**, re-derived at the new location; ADR-0010's rejection **stands**, and its stronger second reason — the gate must not acquire a ruler with no falsifier — is untouched) · **one skill per persona** (the incumbent with a different extension). Costs booked: a preload is **frozen at the session's plugin build** and centralising **does not make a rule current**; a brief that becomes a pointer stops reading as a mandate; **`inventory-counts.test.sh:1410` goes red on the correct tree** if the scratch rule moves, and re-keying it to the skill's text would prove existence and stop proving **reach** — the replacement must key on the **association**, and until it exists **moving a rule out weakens the check**; and **the behaviour test has never been run and has no proxy**. [ADR-0009](./0009-a-skill-description-is-a-trigger-not-a-title.md), [ADR-0010](./0010-a-personas-startup-context-is-a-curated-preload.md), [ADR-0008](./0008-which-layer-carries-a-control.md), [ADR-0005](./0005-plugin-auto-versions-on-merge.md) and [ADR-0002](./0002-agentic-dev-loop-architecture.md) **cited, not amended** — including one **falsification carried but deliberately not written as an amendment**: ADR-0010's *"there is no path to read"* is false (`printenv CLAUDE_PLUGIN_ROOT` → exit 1, **and** the plugin root is derivable from `PATH`, both re-derived in the shell that wrote the record), which makes its exclusions deferrals rather than deprivations — left for that record's own amendment because `harness-lead` is mid-audit on the same clause. **Explicitly undecided:** **the outcome of the ordered review — this record supplies the criterion and the measurement and decides NO individual file** (62 decisions: assign · rewrite so a profile can be assigned · archive · cut), whether and when the reverse assertion is added, what `archive` is mechanically, whether the workload-mirroring files are rewritten or cut, how many methodology skills there should be, the `new-issue` removal, and `harness-lead`'s `skills: []` · **amended 2026-08-13** (a **fifth disposition**, scoped to the three technical families and leaving the four-way framework untouched for the 14 process files: `infrastructure` (21 files), `backend` (19, correcting a relayed `18`) and `frontend` (15) — **55 files total, not the four-way disposition per file** — each consolidate into **one skill per family**, named `cloud-infrastructure`, `backend` and `frontend`. Driver: [#177](https://github.com/tedeuxx/tadeumendonca-skills/pull/177)'s mechanical floor — no subagent has on-demand skill invocation, only forced `skills:` preload or a manual `Read` the model may or may not decide to make — so what needs **forcing into every dispatch** is the process/workflow layer (14 files, this repo's own stated differentiator), while the 55 technical files are reference-only, reached by `Read` when judged relevant, and served better as one greppable entry point per family than as 55 to first guess between. **The density bar (`vpc`, cited in `CLAUDE.md`) is not lowered by folding** — it moves from file-grain to section-grain within `cloud-infrastructure`. **Uniform curation criterion across all three** (a late refinement, incorporated rather than scoped to `backend` alone as first framed): each consolidated file keeps **the owner's own preferred pattern per concern**, already stated today at finer grain across the source files — not an exhaustive merge of every option each one discussed — using ADR-0011's own documentation-versus-standard test as the noise/signal filter, applied at paragraph grain. `backend` carries the sharpest version of the cost since it documents a **retired** architecture with no live consumer to exercise the kept pattern; `cloud-infrastructure` and `frontend` are exercised live. **Explicitly undecided:** the internal structure of `cloud-infrastructure` (organizing axis for 21 services); the per-file assignment of #192's remaining 8 process files; and whether any of the three consolidated files earns a preload slot on any profile) |

| [0012](./0012-issue-type-is-the-routing-axis-and-is-exclusive.md) | The dev-loop has three issue types — `product` / `content` / `loop` — and they are the **routing axis, exclusive**, not the granularity axis | accepted · **the vocabulary was already half-real**: `README.md`'s diagram and `dev-loop/SKILL.md`'s label table already route on `product`/`content`/`loop` in prose, while `gh label list` returns only **5** project labels and no `loop` — so the diagram asserted a decision the label vocabulary never made. Chosen: type is the routing axis, exclusive (one issue, one type); rejected for now, as a later ADR's subject: type as **granularity** (story/task/proposal) — orthogonal, not disqualified, deferred so this record does not conflate two axes. **Four corollaries, each forced by making the routing axis real:** (1) `/autonomy-on`'s queue predicate becomes **`(product OR loop) AND ready`** — measured, its old `product`-only predicate would have silently emptied the drainer in this repo, where **12 of 13** open issues carry `product` and none carries the fictional `loop`; `commands/autonomy-on.md:15` needs updating, not done here. (2) The three types are **exclusive** — `commands/new-issue.md`'s "apply product or content" step becomes a single choice, and the **8** dual-labelled issues in `tadeumendonca-io` are a migration, not a state to preserve — neither edited here. (3) `content` gets a mechanical builder: **owner override** of [#161](https://github.com/tedeuxx/tadeumendonca-skills/issues/161)'s own "measure before adding a persona" precondition — a `writer` persona ships now, tracked at [#187](https://github.com/tedeuxx/tadeumendonca-skills/issues/187), because measured **2 of 13** pure-`content` issues in `-io` have ever closed against **6 of 8** dual-labelled ones, and neither existing profile builds `content` mechanically (`product-lead` has no `Write`; `developer` has `Write` but is never dispatched there). **`loop` passes the same "something must query it" test `type:*` failed, and explicitly because of corollary 1** — the query is what makes it different from retired vocabulary, not a new label added by fiat. **Named, not decided:** relabeling is ungated today (`gh issue edit`/`gh label` sit in the committed `allow` list, neither guard script keys on them) — left an **open question**, not silently accepted, since closing it is a which-layer-carries-this-control question (ADR-0008) rather than a routing one; the `README.md` diagram correction owed (`loop` needs to route to **both** `harness-lead` and `tech-lead`, not the single edge drawn today, since `tech-lead` is the sole ADR author and `loop` issues are the ones most likely to produce one) — named, not edited, per this repo's own citation discipline; a **forward reference** to ADR-0015's subject (whether `harness-lead` becomes an implementer of `loop` changes) — the rejected-for-later alternative of routing `loop` to `harness-lead` alone for both proposing and building is named so it is not silently reintroduced; and a one-line collision pointer for the later task-as-Issue-child ADR against `wip-guard.sh`'s overlap rule. [ADR-0011](./0011-a-skill-exists-to-be-assigned-to-a-profile.md), [ADR-0002](./0002-agentic-dev-loop-architecture.md) and [ADR-0008](./0008-which-layer-carries-a-control.md) **cited, not amended** |

| [0013](./0013-the-orchestrator-is-a-named-role-not-a-persona.md) | The orchestrator is a named role — one term, listed duties, a stated boundary — not a persona | accepted · converges **five** live spellings (`orchestrator` · `main session` · `main loop` · `main agent` · `invoking context`) on one term for new writing, without renaming existing occurrences. Duties named together for the first time: dispatch, commit/push, the `ready` label, the ADR-0012 routing label, and the dispatch-omission judgment call (measured on `-io`#338 per ADR-0006, `:523`). Boundary stated in two parts, not one: **mechanically enforced** for merge and trunk push (`permission-guard.sh:118`, `agent_type` empty for the main agent, rules 7/7b fire against it — asserted at `permission-guard.test.sh:143,221-228`, corrects a suspicion the guard might be blind here; it is not, since 2026-08-03) versus **not enforced and named as such**: `gh issue edit`/`gh label` sit unscoped in the global `allow` with zero matching rule in the guard (`.claude/settings.json:51,54`; gap self-documented at `permission-guard.test.sh:336`), and the dispatch-omission failure shape (`README.md:259`'s "fails silently and looks identical to a clean one") is named as the boundary's blind spot rather than folded into "never decides the irreversible," which does not cover an invisible omission. Rejected: **status quo** (the naming drift is active, not hypothetical, and the one existing boundary statement at `README.md:150` is not the orchestrator's own loaded context per `inventory-counts.test.sh:1397`'s own rule) · **a full `agents/*.md` persona brief** (it is not dispatchable, it is the dispatcher; satisfies none of ADR-0002 amendment #10's four reasons a persona exists). **Consequent work named, not done here** (this record's write scope is `docs/adr/**`): add the duty list/boundary to CLAUDE.md, the orchestrator's own loaded context, so the definition is reachable rather than decorative; close or explicitly re-affirm the label-scoping gap as accepted-but-unclosed (a which-layer-carries-this-control question, ADR-0008); add "(via orchestrator)" to `README.md:177`'s `MR --> QA` edge label. [ADR-0002](./0002-agentic-dev-loop-architecture.md), [ADR-0006](./0006-a-verdict-owed-to-another-persona-is-an-artifact.md), [ADR-0008](./0008-which-layer-carries-a-control.md), [ADR-0010](./0010-a-personas-startup-context-is-a-curated-preload.md), [ADR-0011](./0011-a-skill-exists-to-be-assigned-to-a-profile.md) and [ADR-0012](./0012-issue-type-is-the-routing-axis-and-is-exclusive.md) **cited, not amended** |

| [0014](./0014-a-task-is-an-issue-child-not-a-checkbox.md) | A task is an Issue **child** — own Issue, own branch, own PR, `Parent: #N` in the body — not a checkbox on the story | accepted · resolves a live contradiction: `agents/developer.md:111-141` already reads as though a task is a real Issue with its own MR, while `wip-guard.sh:74-77` (struck, retired 2026-08-04) measured the checkbox model as the one that actually ran, and never produced a task branch across roughly **90**. Chosen: task-as-child. Rejected: **status quo checkbox model** (zero task branches ever, in ~90 — the more defensible reading is the model was simply never instructed, not tried and failed, but rejected anyway since a checkbox has no MR for the DoD already in force to gate) · **reinstating hook-side `Parent: #N` verification** (`permission-guard.sh:843-858`, struck 2026-08-02, shows four correct-in-sequence fixes to exactly this mechanism before it was deleted for a reason orthogonal to whether the parent is a checkbox or a real Issue — intent is not recoverable from a command string, ADR-0004). **Ships restrictive, not the full mechanism:** the sibling-file overlap exemption `wip-guard.sh` deliberately removed (struck prose at `:55-101`, with its own four documented defects) is **not rebuilt here** — sibling tasks touching the same file are **blocked** until a later slice rebuilds it, failing safe over reintroducing a caught bug class. **Closes ADR-0012's own named forward pointer:** a task inherits the parent story's routing type and `ready` state entirely — no independent label of any kind — since `agents/developer.md`'s task-filing rule already checks only the **parent's** `ready` and a task is a decomposition of already-routed work, not a new intake decision. Because no task label is introduced, **ADR-0013's `gh issue edit`/`gh label` scoping gap is inherited, not widened** — named, not closed. **Named as consequent work, not designed here:** the exemption rebuild (with a named non-negotiable test fixture — a mixed open set with a fieldless entry first, the shape the historical vacuous test could not catch); `quality-assurance` gaining a Read-only `gh issue view <parent> --json labels` check during task-MR review; and `agents/quality-assurance.md` gaining sibling-PR awareness (grepped and confirmed absent today) before cross-task drift is flagged as a finding. [ADR-0002](./0002-agentic-dev-loop-architecture.md), [ADR-0004](./0004-autonomy-and-permission-model.md), [ADR-0012](./0012-issue-type-is-the-routing-axis-and-is-exclusive.md) and [ADR-0013](./0013-the-orchestrator-is-a-named-role-not-a-persona.md) **cited, not amended** |

| [0015](./0015-harness-lead-implements-the-harness-it-reviews.md) | `harness-lead` becomes the owner's **implementer** on the machinery it reviews — unscoped `Write, Edit` mirroring `tech-lead`'s precedent, mitigated purely by "cannot merge," not a path-scoped deny | accepted · reverses [ADR-0002](./0002-agentic-dev-loop-architecture.md)'s tenth amendment on the *advisory, pre-implementation* point (amended there, in place, as amendment #11 — struck, not rewritten; the **merge** and **MR-review** clauses of that decision stand verbatim). Driven by a `harness-lead` stress test of this proposal about **itself**, six findings (F1–F6), all re-verified independently in the record. **Capability shape (F6):** `agents/harness-lead.md:4` gains `Write, Edit`, the exact set `agents/tech-lead.md:4` already holds; **no new hook** — `hooks/hooks.json:3-5` registers `PreToolUse` on `Bash` only, so `Edit`/`Write` are invisible to the hook layer for every persona holding them, `tech-lead` included, today. **Rejected:** a path-scoped `Edit`/`Write` deny — **already measured not to hold**, `agents/quality-assurance.md:100-102`'s `security`/`Edit(.claude/**)` failure, re-verified rather than proposed again untested. **Mechanical trigger (F2):** names the exact criterion text `agents/quality-assurance.md`'s boundary-class list (`:714-718`) needs — a diff touching `hooks/**`/`agents/**`/`skills/**`/`commands/**`/`.claude/**` requires a `harness-lead` verdict marker present before it may classify safe, else boundary class regardless — **not edited here**, consequent work. **Durable verdict (F3):** an `<!-- harness-lead-verdict -->` Issue-comment marker (ADR-0006's shape) against a **commit SHA** rather than a PR head, since a harness scenario often predates any PR; names the un-examined `permission-guard.sh` posting-boundary gap as consequent work, not designed here. **Owner-only `loop`-ready (F1, F4):** closes [ADR-0012](./0012-issue-type-is-the-routing-axis-and-is-exclusive.md)'s own named-and-left-open question — `loop`-typed `ready` is an **owner-only** transition, `product`-typed `ready` unchanged (the two leads, per ADR-0012). This is the mechanism that converts "separate Issue for proposal vs. build" from prose into a real gate: the build dispatch cannot start until the owner has touched the proposal artifact. **Tracker entry (F5) is argued as a COROLLARY of F1/F4, not an orthogonal decision** — the `ready` gate has nothing to attach to without an Issue; `dev-loop/SKILL.md:43-53`'s current "does not put it in the tracker, no Issue" rule is reversed, named as consequent work, not edited (outside `docs/adr/**`). Filing itself is unchanged in **who** does it — `harness-lead` stays denied `gh issue create` (rules 5d/7b's catch-alls, re-verified at `permission-guard.sh:135-136`, need no new rule either direction); the orchestrator files it, asked, per *"Review does not open work"*. **The two execution-defect bugs** (`agents/harness-lead.md:170` instructing a `Write` tool the persona didn't have; `:27-42`'s scratch-file section silent about the same gap) travel in the **same** implementing PR as the `tools:` edit, not a separate pre-fix — named, not designed. **Cost accepted explicitly:** `harness-lead` now reviews and builds the same object, the identical "nobody observes the gate that signs the merge" shape `quality-assurance` already named as its own cost when `security` merged into it (ADR-0002, tenth amendment, Decision 2) — mitigated the same way, by "cannot merge," not by a second internal reviewer. `loop`-ready is owner-only **by instruction**, not floor-enforced — left open, not pretended closed. [ADR-0006](./0006-a-verdict-owed-to-another-persona-is-an-artifact.md) and [ADR-0008](./0008-which-layer-carries-a-control.md) **cited, not amended** |
| [0016](./0016-archive-is-docs-archive-not-a-skills-flag.md) | A skill's `archive` disposition is a **file move to `docs/archive/`**, not a frontmatter flag left inside `skills/` | accepted · settles the mechanism [ADR-0011](./0011-a-skill-exists-to-be-assigned-to-a-profile.md) deliberately left open for its `archive` disposition, ahead of [#192](https://github.com/tedeuxx/tadeumendonca-skills/issues/192)'s first file to reach it. Chosen: `git mv` to `docs/archive/<family>/<name>.md` (preserves history), drop the entry from `plugin.json`'s `skills` array, prepend a one-line provenance note. **Rejected:** a frontmatter flag left inside `skills/` (leaves `inventory-counts.test.sh`'s deferred reverse assertion — *"every `SKILL.md` under `skills/` is declared"* — permanently unwritable without a growing per-file exception list) · a separate plugin (disproportionate distribution surface for overflow, not a second product) · leaving the file in place with no marker (indistinguishable from a file simply forgotten from the array, the exact failure ADR-0011 exists to prevent). **Accepted cost:** every archived file needs an inbound-reference check before the move lands, since other skills point at these files by name (`(see routing)`-style); real per-file audit work, not deferred. **Named as consequent work, not done here:** `skills/workflow/documentation-standard/SKILL.md` does not yet name `docs/archive/` as a placement category. [ADR-0011](./0011-a-skill-exists-to-be-assigned-to-a-profile.md) **cited, not amended** |

| [0017](./0017-adr-authorship-is-split-by-domain-not-tech-lead-exclusive.md) | ADR authorship is split by domain — `tech-lead` writes product/system records, `harness-lead` writes loop/machinery records | accepted · corrects the coupling ADR-0012's own routing row already named as a cost: `tech-lead` being the sole ADR author was why `loop`-typed intake kept pulling it in, regardless of stake. **Chosen:** `tech-lead` authors product/system-architecture ADRs (including methodology decisions with product-architecture consequence); `harness-lead` authors pure loop/harness/machinery ADRs; `harness-lead` gains the `adr` skill as a narrow second exception to its otherwise-empty preload (format/process standard, not a description of its own machinery, so it doesn't reopen the staleness concern). **Rejected:** keeping `tech-lead` sole author and fixing only the stale routing table (treats the symptom, not the cause) · granting every persona domain-scoped authorship (no evidence any other persona originates architecturally-significant decisions of its own). **Accepted cost:** a straddling decision has no mechanical resolution, only a convention — co-citation in the `Deciders` line, mirroring ADR-0015's own header shape; role-stacking compounds further for `harness-lead` (proposer/reviewer + implementer + now record-author on its own harness changes), named as an extension of ADR-0015's already-accepted cost, not a new one. **Bootstrapping note carried in the record itself:** under the rule it establishes, this ADR is `harness-lead`'s to write — it was instead authored directly by the owner and the orchestrating session while the plugin was temporarily disabled, no persona dispatched, and the record says so explicitly rather than silently exercising the authority it grants. [#223](https://github.com/tedeuxx/tadeumendonca-skills/issues/223), [ADR-0012](./0012-issue-type-is-the-routing-axis-and-is-exclusive.md), [ADR-0015](./0015-harness-lead-implements-the-harness-it-reviews.md) |

| [0018](./0018-permission-entries-have-three-states-and-absent-is-not-one.md) | A permission entry is `deny` / `ask` / `allow` — **absent is not a fourth state**, it is the omission of a decision | accepted · generalizes [ADR-0004](./0004-autonomy-and-permission-model.md)'s narrower, earlier finding (*"a control expressed as absence is not a control"*, earned on `gh api` being unlisted rather than denied) into a stated principle, driven by [#163](https://github.com/tedeuxx/tadeumendonca-skills/issues/163)'s measurement of an owner request to make a class of AWS commands "unitarily permitted." **Chosen:** three real states only; "unitary" has to resolve to an actual `allow` (if the class is judged safe unconditionally) or stay `deny` — there is no third resting place that also counts as a decision. **Rejected, each on a specific measured failure:** a fourth "absent" state (erasable by any broader `allow` added anywhere, silently — this workspace already carries 15 such unreviewed entries in local overlays; a removed `deny` and one that never existed are indistinguishable in the file; does not survive `settings.local.json`, which accumulates by clicking) · migrating the unitary case into `permissions.ask` — **tested live, not assumed**: #163's one-minute test moved an entry to `ask` in both the committed project file and the global user-scope file, then ran the command as a real tool call. **Result: no permission prompt, the command executed directly against real AWS auth** — falsified, in this Claude Code version, through this configuration surface. **Accepted cost:** `ask` is now vocabulary with no working implementation until re-measured against a newer harness version; any decision that would have used it is stuck choosing between `deny` and `allow` meanwhile. Does not resolve #163's own remaining action (removing 35 duplicated AWS `deny` entries from the unversioned, self-protected global `~/.claude/settings.json` — an owner-executed step with no PR, gate, or record), which is orthogonal to the vocabulary this record settles. [ADR-0008](./0008-which-layer-carries-a-control.md) **cited, not amended** — 0008 decides which *layer* carries a control; this decides which *state* an entry can be in. **Bootstrapping note carried in the record itself**, same shape as ADR-0017: loop/harness machinery is `harness-lead`'s domain to author per that record's own rule, but this one was written directly, no persona dispatched, plugin disabled for this phase |

| [0019](./0019-readme-is-the-single-source-of-truth-for-the-dev-loop.md) | `README.md` is the single source of truth for the dev-loop documentation — `docs/dev-loop-design.md` is retired to a pointer stub | accepted · amends [ADR-0002](./0002-agentic-dev-loop-architecture.md)'s "where the design now lives, harness-agnostically" note, which had named `docs/dev-loop-design.md` as that home. Driven by [#261](https://github.com/tedeuxx/tadeumendonca-skills/issues/261), corrected mid-flight from "shrink the README" to "consolidate three overlapping documents into one." **Chosen:** README absorbs the content `dev-loop-design.md` carried at greater depth — the Definition of Done (issue-requirement enumeration, the finding-blocks-only-with-a-falsifier rule, severity set by the lens that found it), the intake-formalism argument (external ruler, the two-round budget, parallel-not-serial dispatch), and a new "What travels if this design moves to another harness" section carrying the essential/incidental/known-weak split. Content already redundant (the roster narrative, the branch diagrams) is not duplicated a second time. **Rejected:** keeping `dev-loop-design.md` canonical and thinning the README (the owner's own original framing, reversed — a thin README just relocates the two-sources problem) · `git rm` outright (breaks the file's own cited canonical URL with no redirect; ~~this repo's supersede-never-delete convention already answers the question~~ — **struck 2026-08-15**, see this record's own amendment: [ADR-0020](./0020-an-adr-earns-its-place-by-explaining-the-current-codebase.md) replaced that convention, the rejection **stands on its surviving reason** (the canonical URL resolving), and ADR-0020 is scoped to ADR records, which a redirect stub is not). **Accepted cost:** the README is now the longest document in the repo, and the design doc's "import into any harness" framing is a pointer one hop from the content rather than the content itself. [ADR-0017](./0017-adr-authorship-is-split-by-domain-not-tech-lead-exclusive.md) — `harness-lead`'s domain to author, a pure loop/documentation-architecture decision |

| [0020](./0020-an-adr-earns-its-place-by-explaining-the-current-codebase.md) | An ADR earns its place by explaining the **current** codebase — the disposition of a reversed decision is one of three, keyed on `status` | accepted · replaces **supersede-never-delete**, which was never itself recorded as an ADR (no `Decision outcome` in this library; it lived in `skills/workflow/documentation-standard/SKILL.md` and this index's own closing note, and live records cited it — a set stated by its criterion and never by a count: **two** cite `supersede-never-delete` by name (ADR-0011, ADR-0019), **five** cite the `supersede-*` family (those two plus ADR-0002, ADR-0010, ADR-0016), all `accepted`, all line-wrapped across the hyphen). Driven by [#281](https://github.com/tedeuxx/tadeumendonca-skills/issues/281). **Which arm of the significance gate fires, corrected in the record itself:** not *alters a previously-recorded decision* (nothing was recorded) but **sets a cross-cutting pattern** — it governs both ADR libraries, prescribes `git rm` against artifacts a published `-io` page renders, and live records cite the convention it replaces. **Only ADR-0019's citation is about the arm this record replaces; the other four (ADR-0002, ADR-0010, ADR-0011, ADR-0016) each justify a strike inside a live record and are explicitly not to be swept.** **Chosen:** three dispositions — delete with a mandatory `README.md` History row · fold the context into the superseding record (`## What this replaced`) **before** the deletion · keep the file where the record is `proposed` or merely unexercised. Both preconditions are preconditions, not follow-ups. **Rejected:** keeping supersede-never-delete (not wrong about the risk; answers it with a marker the misreading consumer — an agent, which reads bodies not status lines — does not read) · moving retired records to `docs/archive/` as [ADR-0016](./0016-archive-is-docs-archive-not-a-skills-flag.md) does for skills (that mechanism works because a gate computes over the directory boundary; **nothing keys on the `docs/adr/` boundary**, so moving a record out of it changes no gate's outcome — machinery does read the directory (`inventory-counts.test.sh` greps `docs/adr/0008` by path, `docs-test.yml` filters on `docs/**`), which makes the why-not stronger: an archived record is still scanned by the floor-claim assertion, so the move buys the appearance of the remedy and not the remedy) · building a gate now (deferred on evidence — the deletion set in this library is **empty**: **19 records before this one** / 18 accepted / 1 proposed / **0 superseded**, and 20 / 19 / 1 / 0 once it lands, measured at `2de6844` with `ls docs/adr/0*.md | wc -l` and `grep -ih "^- \*\*Status:\*\*" docs/adr/0*.md` — the figure the deferral rests on is the last one, zero superseded). **Scope boundaries that are part of the decision:** whole records only — inside a live record, append-and-strike is **unchanged and load-bearing** (`inventory-counts.test.sh`'s roster-membership assertion reads those markers, and stripping them was measured to make it *quieter*, not red) — and *reversed* decisions only, never unbuilt or unexercised ones. **Accepted costs:** the reasoning of a reversal is no longer preserved at length · **nothing enforces any of it** (a record deleted from a scratch copy of this tree left the suite at `69 passed, 0 failed`) · the first real execution is `-io`'s library, where `AdrTable.test.tsx`'s `inLibrary > 5` assertion reddens against 8 superseded-by-status records, and `architecture.{en,pt}.md` states the old rationale as published copy — `product-lead`'s blocking call, not settled here · **three further `-io` costs the record now names so its list is the whole executor brief**: `-io`'s own `docs/adr/README.md` still publishes *"never deleted"* (it shares no vocabulary with `supersede-never-delete`, so every sweep missed it), disposition 1's designated home there is a History table whose heading asserts the rule it replaces and whose every row links a live file, and `architecture.{en,pt}.md` hard-links five superseded records by URL — in the table under *"O que foi cortado — e tinha sido construído antes, que é a parte que importa"*, serving the argument in the *"Se você precisar do backend de volta"* subsection below it, which cites the five by bare number and depends on them being readable · disposition 1's History table does not exist in this library yet, left open deliberately since the deletion set here is empty. [ADR-0017](./0017-adr-authorship-is-split-by-domain-not-tech-lead-exclusive.md) — `harness-lead`'s domain to author, a pure loop/documentation-practice decision |

New ADRs: copy `template.md` → `NNNN-kebab-title.md`, next number in sequence. ~~Never delete a superseded
ADR — mark it `superseded by ADR-XXXX` and link forward.~~ **Struck 2026-08-15 (#281).** The disposition
of a reversed decision is now one of three, keyed on the record's `status` field and never on its
filename — delete with a mandatory History row, fold the context into the superseding record first, or
keep the file — per [ADR-0020](./0020-an-adr-earns-its-place-by-explaining-the-current-codebase.md) and
the operative wording in `skills/workflow/documentation-standard/SKILL.md`. **This applies to whole
records only**: inside a live record, amend by appending and strike in place, never rewrite — unchanged.
